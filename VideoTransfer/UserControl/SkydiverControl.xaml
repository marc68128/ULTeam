<UserControl x:Class="VideoTransfer.UserControl.SkydiverControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <Image Margin="0" Grid.Row="0" Stretch="UniformToFill" HorizontalAlignment="Center" Source="{Binding Image, Mode=TwoWay}"></Image>
        <ProgressBar Margin="0" Foreground="LightSkyBlue"  Opacity="0.8" Grid.Row="0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Orientation="Vertical" Maximum="10" Value="8" />
        <Button Grid.Row="1" Command="{Binding InitializeCommand}">Initialiser</Button>
    </Grid>
</UserControl>
