<UserControl x:Class="VideoTransfer.UserControl.SkydiverControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <Button Grid.Row="0" Command="{Binding InitializeCommand}">Initialiser</Button>
        <Image Margin="0" Grid.Row="1" Stretch="UniformToFill" HorizontalAlignment="Center" x:Name="Image" Source="{Binding Image, Mode=OneWay}"></Image>
        <ProgressBar Margin="0" Foreground="LightSkyBlue"  Opacity="0.8" Grid.Row="1" Height="{Binding Height, Source=Image}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Orientation="Vertical" Maximum="100" Minimum="0" Value="{Binding CurrentUploadPercentage, Mode=OneWay}" />

    </Grid>
</UserControl>
